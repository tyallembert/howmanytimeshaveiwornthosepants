/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com'], // Add the Firebase storage hostname
      },
};

export default nextConfig;
